{% extends 'base.html.twig' %}

{% block title %}Message #{{ message.id }}{% endblock %}

{% block body %}
<div class="container mt-4" style="max-width:720px;">
    <div class="card shadow-sm">
        <div class="card-body">
            <div class="d-flex justify-content-between mb-2">
                <strong>
                    {% set author = message.author ?? message.user ?? null %}
                    {{ author ? (author.username ?? author.email ?? 'Anonyme') : 'Anonyme' }}
                </strong>
                <small class="text-muted">
                    {% if message.createdAt is defined and message.createdAt %}
                        {{ message.createdAt|date('d/m/Y H:i') }}
                    {% endif %}
                </small>
            </div>

            <p class="mb-0">{{ message.content }}</p>

            {% if message.image is defined and message.image %}
                <hr>
                <div class="text-center">
                    <img src="{{ asset('uploads/messages/' ~ message.image) }}"
                         alt="Image du message"
                         class="img-fluid rounded shadow-sm mt-2"
                         style="max-height: 400px;">
                </div>
            {% endif %}
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ path('app_home') }}" class="btn btn-link">← Retour</a>

        {# Bouton Modifier (uniquement l’auteur du message) #}
        {% if app.user and app.user == message.author %}
            <a href="{{ path('app_message_edit', { id: message.id }) }}" class="btn btn-warning btn-sm ms-2">
                Modifier
            </a>
        {% endif %}

        {# Bouton Supprimer (auteur ou admin) #}
        {% if app.user and (is_granted('ROLE_ADMIN') or app.user == message.author) %}
            <form method="post"
                  action="{{ path('app_message_delete', { id: message.id }) }}"
                  class="d-inline"
                  onsubmit="return confirm('Supprimer ce message ?');">
                <button class="btn btn-danger btn-sm">Supprimer</button>
            </form>
        {% endif %}
    </div>

    <hr class="my-4">

    <div>
        <h5 class="mb-3">Commentaires ({{ message.comments|length }})</h5>

        {% for comment in message.comments %}
            <div class="card mb-2 shadow-sm">
                <div class="card-body py-2">
                    <div class="d-flex justify-content-between">
                        <div>
                            <strong>
                                {% set cAuthor = comment.author ?? null %}
                                {{ cAuthor ? (cAuthor.username ?? cAuthor.email ?? 'Anonyme') : 'Anonyme' }}
                            </strong>
                            <span class="text-muted small">
                                • {{ comment.createdAt ? comment.createdAt|date('d/m/Y H:i') : '' }}
                            </span>
                        </div>

                        {# Actions: suppression (auteur du commentaire ou admin) #}
                        <div class="d-flex align-items-start">
                            {% if app.user and (is_granted('ROLE_ADMIN') or (comment.author and comment.author.id == app.user.id)) %}
                                <form method="post"
                                      action="{{ path('app_comment_delete', { id: comment.id }) }}"
                                      onsubmit="return confirm('Supprimer ce commentaire ?');"
                                      class="ms-2">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete_comment_' ~ comment.id) }}">
                                    <button class="btn btn-sm btn-outline-danger">Supprimer</button>
                                </form>
                            {% endif %}
                        </div>
                    </div>

                    <div class="mt-2">
                        {{ comment.content }}
                    </div>
                </div>
            </div>
        {% else %}
            <p class="text-muted">Aucun commentaire pour le moment.</p>
        {% endfor %}
    </div>

    {# Formulaire d’ajout de commentaire #}
    <div class="mt-4">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <form method="post" action="{{ path('app_comment_add', { id: message.id }) }}">
                <div class="mb-2">
                    <label for="comment_content" class="form-label">Nouveau commentaire</label>
                    <textarea id="comment_content" name="content" class="form-control" rows="3" required></textarea>
                </div>
                <input type="hidden" name="_token" value="{{ csrf_token('comment_add_' ~ message.id) }}">
                <button type="submit" class="btn btn-primary">Publier</button>
            </form>
        {% else %}
            <a class="btn btn-outline-primary" href="{{ path('app_login') }}">Se connecter pour commenter</a>
        {% endif %}
    </div>
</div>
{% endblock %}
